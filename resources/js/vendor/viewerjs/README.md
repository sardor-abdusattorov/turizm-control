Viewer.js v1.11.6 (MIT) — https://github.com/fengyuanchen/viewerjs

Vendored dist build, registered via FilamentAsset in AppServiceProvider so the
al-saloul/filament-image-gallery plugin finds window.Viewer locally and never
falls back to its unpkg CDN loader (the admin must work on a closed network).
